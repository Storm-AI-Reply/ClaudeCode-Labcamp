.DS_Store
# Local Python envs — labs: `requirements.txt` → `.venv/` | docs: `requirements-docs.txt` → `.venv-docs/`
.venv/
.venv-docs/
# MkDocs `mkdocs build` output — generated static site; rebuild from docs/ + mkdocs.yml
site/

# Local-only: maintainer notes, slides, drafts, scratch (not participant materials)
_maintainer/
__pycache__/
*.pyc
.pytest_cache/
